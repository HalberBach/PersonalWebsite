---
import PageTitle from "./PageTitle.astro";
const { title } = Astro.props;
---

<div class="flex flex-col min-h-screen page-layout-mt p-4 md:p-16 gap-8 md:gap-20 border w-full">
  <PageTitle title={title} />
  <div class="w-full">
    <slot />
  </div>
</div>

<style>
  .page-layout-mt {
    padding-top: calc(4rem + var(--navbar-height));
  }
</style>
